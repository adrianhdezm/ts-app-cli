import * as path from 'path';

export const CLI_ROOT_PATH = path.join(__dirname, '..');
export const TEMPLATES_PATH = path.join(CLI_ROOT_PATH, 'templates');
export const COMMON_TEMPLATE_PATH = path.join(TEMPLATES_PATH, 'common');
